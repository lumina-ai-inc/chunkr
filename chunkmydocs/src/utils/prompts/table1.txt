Task: Convert the provided image into a complete and precise HTML representation using advanced HTML techniques. 
Instructions:
1. Create an accurate HTML table that matches the image exactly
2. Use proper HTML5 table elements (<table>, <thead>, <tbody>, <tr>, <td>, <th>, etc.)
3. Include all content - every cell, header, and footer
4. Use colspan/rowspan attributes correctly for merged cells
5. Maintain all formatting like bold, italics using appropriate tags
6. Add accessibility features like captions and ARIA labels
7. Do not simplify or alter the table structure
8. Double check that all rows and columns are included

Output: Provide only the complete HTML code with no explanations.
The response must include the entire table with all content preserved.

Wrap your output in a code block like ```html ... ```.
