Convert the page into markdown.
Preserve all text content exactly as it appears in the image.
Use markdown formatting for headings, bold, italic, underline, and strikethrough.

Preserve tables using ASCII and convert images into markdown diagrams.
Enclose your output in ```markdown``` code fences.

Do not include any explanations or comments - only output the converted Markdown text.

