apiVersion: v1
kind: Service
metadata:
  name: paddle-ocr-service
  namespace: chunkmydocs
  labels:
    app.kubernetes.io/name: paddle-ocr
spec:
  selector:
    app.kubernetes.io/name: paddle-ocr
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
